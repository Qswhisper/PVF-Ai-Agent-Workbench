# 更新日志

## 未发布

- 暂无。

## 2.2.2

- 修复普通读取结果被误当作变更原文的问题：面向阅读的结果会明确标记不可直接用于修改；原始读取会只读比较 `Cn` / `Tw` 候选，仅在一方明显更可信时自动选择，并公开选择依据。
- 变更集零命中时可区分“使用了展示文本”和“文字编码不一致”，给出从同一路径原始读取重新构建精确 token 的恢复路线，不再让 Agent 猜编码或手工转换简繁体。
- `pvf-change validate` 新增机器可执行交接信息：同一路径的参数与中文保持在同一变更集，连续第二轮明确继承上一轮 `APPLY-MANIFEST.json`，并提供不应擅自改写的下一条命令。
- 新增显式累积修改链：下一轮差异通过 `baseline.applyManifest` 继承上一轮已复查输出，同时持续绑定最初受保护源、完整哈希、源备份与累计改动数量；客户端部署预览会阻止错误基线回退历史改动。
- 修复同文件联动计划无条件把普通结构改动移到安全文字之前的问题；当删除结构依赖先清空完整中文时保留变更数组顺序，同时仍保证同文件文字只重建一次字符串表、修改一次脚本，并统一精确复查最终结果。
- 本机 profile 改存到工作台外的用户状态目录；旧版目录内 profile 首次使用时原子复制且不删除，增加刷新工作台后配置仍可读取的临时夹具自检。
- 同一文件的多条安全文字改为一次追加字符串表、一次修改脚本，相同新文字只追加一份，保留逐条证明和整批证明；100 条与 500 条压力回归通过，避免批量任务反复重建。
- 源 PVF 备份改为按 SHA256 存放：首次生成时完整复制并核对，之后只有哈希一致才复用，减少同一源版本反复生成时的磁盘占用；备份继续位于外部任务输出区。
- 正式写入权限面不再公开旧的独立备份工具，备份只由受控执行器在保存前创建和核对，避免两套路线并存。
- 精简批量核验记录：每条变化只保存自己的证明，整批证明只保存一次，避免大量文字改动让清单近似平方膨胀。
- 相同完整文字在一个文件重复出现时，可用紧邻的前后原文精确定位目标；定位信息会绑定到预演、正式生成和复查记录，定位不唯一或原文变化就停止。
- 上下文只用于定位，不会放宽中文编码、完整文字、字段类型、StringLink、备份或独立复查保护；不支持按出现序号猜位置。
- 普通脚本中的完整中文反引号文本现支持真实多行；受控批量替换要求 `replaceAll=true` 与精确 `expectedOccurrences`，数量不符立即停止。
- 同一 PVF 文件可包含多条联动变化：参数/结构使用普通 `replace-text`，中文使用 `verified-inline-text`，工作台统一计算最终文件并执行临时输出、独立解析和精确读回。
- 参数/结构写入改为原始 token 补丁，避免重新编译全文破坏既有中文或 StringLink；支持数字修改、参数组增删、既有 token 复用及安全追加 ASCII 标签，同时证明旧字符串表条目保持不变。
- 零命中优先返回 `OCCURRENCE_COUNT_MISMATCH`，成功结果不再残留误导性的阻断原因；补齐批量、同文件联动和 Cn/Tw 多行回归。
- `.str`、StringLink 显示文本、部分中文 token、无法编码字符和未精确计数的批量仍保持阻断。

## 2.2.1

- 普通 PVF 脚本中的单个完整中文名称/描述新增受控写入模式，同时支持目标确认的简体 `Cn` 与繁体 `Tw`：预演先按同一编码生成隔离临时输出，使用独立 TypeScript 解析器精确读回，并证明所有既有字符串表条目保持不变；失败时不生成批准码。
- 修复繁体 PVF 被强制交给简体复核后显示乱码的问题；当两种编码冲突且其中一种具有明显乱码特征时，读取保留更可信结果并标出实际编码，写入则失败关闭并提示重新读取。
- 新增繁体 Big5 完整写入回归，以及“`太陽` 被错读为 `び锭`”必须阻断的负控；旧 `verified-inline-cn` 变更集仍可兼容，推荐新模式名 `verified-inline-text`。
- 正式输出继续强制源文件不覆盖、独立备份、输出 SHA256 绑定和生成后复查；中文路线额外要求全文精确一致，不能用排版等价替代。
- 修复 TypeScript 备用解析器复用会话初始编码读取 `stringtable.bin` 的问题，现按每次目标编码分别缓存和解码。
- `.str`、StringLink 显示文本、部分 token、批量中文替换和无法无损编码字符在 2.2.1 中继续失败关闭；新增完整中文正例、HTML 数字实体负控、StringLink 保持和端到端回归。

## 2.2.0

- 面向代码新手增加“通俗结论优先、技术详情后置”的渐进式回复规范；保留全部安全检查，并让 Agent Eval 自动拒绝在开头堆叠 ASCII、dry-run、manifest、SHA 绑定等未解释术语。
- 新增独立的 `workbench.bat client-pvf` 测试部署路线：只接受成功读回且绑定最终输出 SHA256 的 apply 结果，先预览并绑定 profile 客户端当前版本，单独确认且关闭客户端后才安装；输入完整时固定为直接预览的第一条命令，同时允许任意子命令位置的 `--help` 正常返回用法。
- 客户端部署采用内容寻址备份，相同版本只存一份；新 PVF同目录暂存并核对后替换，部署后再次校验，恢复也要单独预览和确认。源 PVF、独立输出和客户端目标必须互不相同，NPK/IMG/UI 始终不在授权内。
- 新增 17 项纯临时夹具部署自检，覆盖错误确认码、过期预览、源客户端冲突、profile/源不一致、路径越界、旧 apply 清单、备份去重、部署、恢复和非 PVF 客户端文件不变；Doctor 与 Release Gate 3 纳入该自检。

- 根据真实命令型 Agent 实战增加规则问答与精确只读任务快速路径，补齐常用 registry 映射；减少无目标扫描、重复帮助、目录试探和不必要的完整知识索引加载，同时保留安全与目标读回。
- 修复统一依赖 Planner 把精确数字 ID 当作模糊关键词的问题：`--id` 只走 domain 对应 registry，`--path` 只走精确路径；怪物 ID 1 的真实样本由 100 个混合节点收敛为 1 个 root 与 3 个直接依赖。
- Tag 查询可直接接收 `observe-pvf` 输出目录；统一 planner 查询默认返回 20 条并附带报告摘要、root 与安全边界，避免读取外部报告或实现源码。
- 精简随包 Skill 的重复说明，Agent Eval 支持顺序无关的必备关键词组合，降低正确回答被固定语序误判的概率。
- 新增 `pvf-read resolve-skill`，在目标 PVF 内一次闭合角色分支、职业技能 registry 与 `.skl`；新增 `pvf-read search-script`，将精确 NUT 声明与目标脚本观察固定为两条命令。
- Planner 与统一查询新增机器可读的完成边界：返回的 JSON 已是完整生成报告，Agent 不再探测输出目录或另写摘要，同时继续声明它不是运行证据或可应用补丁。
- Agent Eval 增加技能两命令路线、NUT 两命令路线和 Planner 停止边界的确定性回归。
- 精确只读路线统一禁止对已明确的工作台、目标 PVF 和输出目录做通用 shell 预检；命令自身负责输入校验和外部目录创建，修复真实 Agent 在正确路线前仍调用 `Test-Path` 的问题。
- Tag 观察增加只读 `query-observation --report` 入口，可直接消费 `observe-pvf` 返回报告；同时明确 APC 的 Planner domain 为 `apc`、`aicharacter` 仅为 registry 名，消除真实 Agent 的两类自然误调用。

- 将随包 Tag 社区层从未经作者边界确认的大批量注释收敛为 344 条作者成员精确匹配、去重且可直接用于任务定位的单行摘要；保留 2513 条官方原文 / 工具扩展存在性记录与 54 条 registry 提示，并移除署名、渠道、联系方式、维护记录、长正文和歌词类无关内容。
- 内置知识生成器增加显式作者筛选、缺少作者条件时失败关闭、摘要提取、单类构建与 dry-run；知识纯净检查新增大小写无关的私有元数据、来源披露、旧客户端角色和历史来源名称阻断。
- 技能参数事实升级到 2.2，删除无执行价值的纯净度自证字段；删除两份冗余产品功能总表，将仍有执行价值的七类横向能力并入正式任务路由。
- 客户端兼容矩阵改用稳定功能基线、SHA 研究基线和兼容压力上界三种中性角色，保留原有只读比较能力和证据边界，不再携带具体客户端名称。

- 将 PVF 内部路径统一改为失败关闭校验，拒绝绝对路径、盘符/UNC、ADS、空段、`.`/`..`、Windows 设备名和落盘越界；副本提取使用独占新文件，旧导入器的 apply/源覆盖入口改为强制转交受控写入通道。
- 普通 backend server 即使 native 可用也默认只读；只有 `pvf-change apply` 的受控 capability 才公开写工具，且源 PVF、已有输出、已有备份与已有 manifest 均禁止覆盖。
- 新增自动 `Cn` 语义读取保护：中文脚本搜索、`.str`、StringLink 和含非 ASCII 的脚本会在统一后端入口自动使用 TypeScript 只读语义结果，无需新增 profile 或用户参数。
- 受控写入增加中文编码护栏与会话文本 overlay：数字/ASCII 最小修改可保持 StringLink 文本并读回；`Cn .str` 和直接非 ASCII 文本写入在 dry-run/apply 前阻断，避免 native 返回成功但输出乱码。
- 索引建立时记录完整 PVF SHA256，日常 freshness 继续使用元数据与三段采样快速检查，高可信验收可用 `--verify-full-sha`；同名的多个 `Script.pvf` 使用路径哈希隔离缓存键。
- fallback 自检扩展到 66 项，加入便携 GBK fixture、语义搜索、受控写入负控和路径攻击样本；大型自检、Doctor、backend contract 与 Release Gate 默认只输出摘要，完整报告落盘，失败项仍直接显示；未配置可选 profile 不再报成警告。

## 2.1.1

- 修正受控 `replace-text` apply 对 PVF 编译器排版规范化的 readback 假失败：保留全文 SHA，新增区分大小写且仅允许 Section 外空白、数据换行与 float32 展示等价的 token 校验；manifest 分列 exact、normalized-equivalent、binary 与 failed 数量，并补齐 UI/AIC 正例及标签、整数、字符串负控。

## 2.1.0

- 将只读备用后端的运行源码从 JavaScript 迁移为由固定 Node.js 24 runtime 直接执行的 TypeScript；不增加 npm、联网或构建步骤。
- 只读模式的 stdio 工具枚举改为仅公开读取工具，并在分发层与 TypeScript API 层继续以 `READ_ONLY_FALLBACK` 阻断所有写入口。
- 扩展合成 fixture 自检，覆盖 TypeScript runtime 身份、native 优先选择、写工具隐藏、direct API/stdio 写负控和源文件 SHA 不变。
- 增加 TypeScript 只读后端机器契约，固定源码闭包、公开/阻断工具、直接 API 阻断和会话、读取、搜索资源上限。
- 畸形文件树、重复规范化路径和数据 checksum 失败改为失败关闭；搜索结果显式报告截断、读取错误数和有限错误样本。
- 为 StringTable、脚本 token 和 StringView 增加解析前资源上限，并补齐 `.lst` 相对登记路径按注册表目录解析的闭环。
- 真实多版本 PVF 差分修正 `.lst` 显示文本与登记解析的分层、根目录登记路径的 `./` 前缀，并将 StringTable/StringView 改为有界按需解码以降低大型 PVF 常驻内存。

## 2.0.0

- 普通任务改为完全自包含：随包提供固定 Node.js runtime 与 native PVF backend，不依赖外部 MCP、编辑器插件或已下架工具。
- 统一 `workbench.bat` 入口，覆盖环境检查、只读读取、索引、受控 change-set、dry-run、显式授权输出、备份和 readback。
- 知识包收口为百科、字段词典、工作流、任务卡和轻量路由；删除研究账本、历史验收报告、来源定位、样本统计和机器路径。
- 内置 NUT 声明、PVF tag 可信分层和常用任务书签；普通任务无需携带额外知识目录。
- 增加全包质检、语义比较、LST 生命周期、掉落整理、物品来源、技能树安全合并、原子内容生成与统一依赖预览。
- 增加副本与世界标准化路线，包括 map 宽屏结构迁移、worldmap 接口布局、深渊组、难度表、地狱名单、城镇预览和 ANI 边界。
- 增加外部 SHA 锁定的 PVF 谱系与客户端兼容矩阵；所有报告、缓存和真实路径保持在 Workbench 外。
- 增加项目级 `dnf-pvf-xpilot` Skill 适配器，同时保留无 Skill 宿主直接读取 `AGENTS.md` 的路线。
- 增加 runtime 完整性、知识语义纯净性、Agent eval 与三段 portable release gate。
- native 通过完整性校验但因 VC++ v14 x64 运行库加载失败时，人工终端会打开微软官方说明页；Agent/CI 只打印链接，不弹窗或自动安装。
- 增加无 npm 依赖的纯 JavaScript 只读备用后端：native 加载失败时自动接管文件树、脚本/LST/StringLink、NUT、二进制 ANI、搜索和注册表读取；所有备份、apply 与保存以 `READ_ONLY_FALLBACK` 硬阻断。
- 增加合成 PVF 双后端自检、stdio 集成负控和外部多 PVF native/fallback 差分工具，并纳入无 PVF Release Gate 3。
- 版本号从公开基线直接升级为 `2.0.0`。

## 1.1.0

- 早期公开版本。
