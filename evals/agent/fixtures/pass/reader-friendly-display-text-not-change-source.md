这不是“必须自动试遍所有写法”的信号，也不能据此认定同一文件存在混合编码。普通阅读显示可能为了好读把繁体转成简体，所以看到的“准备时间”不等于修改校验使用的原文；这次 0 命中是工作台主动安全停止，没有生成修改版。

应对同一路径重新原始读取，采用提示的繁体编码，从完整原始 token 重建 `previousText` 和 change-set。不要手工转换繁简体，也不要让工具自动穷举字形或跨编码写入。

技术详情（通常不用看）：运行 `pvf-read read --raw --pvf-encoding Tw`，原文应以这次返回为准；例如显示视图的“准备时间”可能对应 raw 结果里的“准備時間”。如果仍不精确命中就继续停止，不给 approval code。`.str`、StringLink 文字仍保持只读，`.lst`、`.co` 和 NUT 也没有因此放开。
