可以继续调查这项独立改名，但现在不能证明其他文件逐字节不变；还未执行预演和独立往返核验。

技术详情（通常不用看）：
`13` 是 namespace（文字表编号），不是 token 类型编号；键为 `name_key`。底层 type9/type10 分别承载编号和键，二者不构成类型冲突。明确改名使用 `verified-stringlink-detach`，仍须完成预演、临时独立往返和生成后的独立读回，保留共享文字表与既有字符串条目，实机检查最终名称显示。
