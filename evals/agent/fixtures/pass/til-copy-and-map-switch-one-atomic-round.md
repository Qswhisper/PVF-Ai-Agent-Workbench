既有 `.til` 的普通字段修改仍不开放；`RAW_ASCII_FILE_TYPE_UNSUPPORTED` 是正确的安全停止，原始参数扩展名保护保持不变。

这里不必拆成两轮。可以在同一 change-set 中用 `copy-file` 复制正确 TIL 到不存在的新路径，同时对 18 张既有 `.map` 做精确引用替换。只禁止在这一轮对刚复制的新 TIL 本身混入 `replace-text`；只有修改克隆内容本身才需要以上一轮成功清单开始累计。

随后先做预演（检查方案；中文改动会用临时文件验证并立即清理），获得授权后生成独立的修改版 PVF，并在生成后重新检查；客户端仍需单独授权。
